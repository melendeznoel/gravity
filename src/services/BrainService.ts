import { IMessageData } from '../models'
import { MesssageStatusEnum } from '../enums'

export class BrainService {
  async isGoodMessage (message: string): Promise<IMessageData | null> {
    return null
  }
}
